<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="PlantCare.App.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:PlantCare.App.Views"
    Title="PlantCare.App"
    Shell.FlyoutBehavior="Disabled">

    <!--<FlyoutItem>
        <Tab>
            <ShellContent
                Title="Home1"
                ContentTemplate="{DataTemplate views:HomeView}"
                Route="overview" />
            <ShellContent
                Title="Home2"
                ContentTemplate="{DataTemplate views:HomeView}"
                Route="overview" />
        </Tab>
    </FlyoutItem>-->

    <!--<Shell.TitleView>
        <views:LogoView />
    </Shell.TitleView>-->

    <TabBar>

        <ShellContent
            Title="My Plants"
            ContentTemplate="{DataTemplate views:PlantOverviewView}"
            Route="overview" />
        <ShellContent Title="Reminders" ContentTemplate="{DataTemplate views:ReminderView}" />
        <!--  Icon="reminders.png"  -->
        <ShellContent Title="Settings" ContentTemplate="{DataTemplate views:SettingsView}" />
        <!--  Icon="settings.png"  -->
    </TabBar>
</Shell>